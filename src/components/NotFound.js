import React from 'react';

// make NotFound a stateless functional component
// this component is rendered only when the url doesn't match any declared routes (<Miss />)
const NotFound = (props) => {
    return (

        <h1>
         NOT Found !!!!
        </h1>
    )
 }


export default NotFound;
